[H1] Custom Text Classification Azure AI Language Service ## ProblemYou need classify text documents categories using Azure AI services, including:-Custom single-label classification(only one category per document)-Custom multi-label classification(multiple categories per document)## Solution AzureUseAzure AI Language Service - Custom Text Classificationbuild, train, deploy, consume classification models text data.## Components Required- Azure AI Language resource- Labeled dataset (training & testing)- Azure AI Language Studio REST API- Authentication key (Ocp-Apim-Subscription-Key)- REST API endpoints training, deployment, inference## Architecture / Development### Project Lifecycle#### 1. Define labelsIdentify categories classification (e.g., "Action", "Adventure", "Strategy").#### 2. Tag dataLabel text documents corresponding categories.-Single-label: One category per document-Multi-label: Multiple categories per document#### 3. Split datasets-Training: ~80%-Testing: ~20%-Options: Automatic manual split#### 4. Train modelSubmit training request via API:```jsonPOST <YOUR-ENDPOINT